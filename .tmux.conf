# Prefix: Ctrl-a. Press it twice to send Ctrl-a to the application.
unbind-key C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Split at the current pane's working directory.
# - creates top/bottom panes; | creates left/right panes.
unbind-key '"'
unbind-key %
bind-key - split-window -v -c '#{pane_current_path}'
bind-key | split-window -h -c '#{pane_current_path}'

# Vim-style pane navigation and resizing (prefix, then key).
# Movement is not repeatable so typing right after switching panes goes to the pane;
# resizing keeps -r so H/J/K/L can be pressed repeatedly.
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5

# Vim copy mode: prefix [, then v to select, y to copy, q to leave.
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'pbcopy'

# Keep Escape responsive in Vim/Neovim.
set-option -s escape-time 10

# Pass Ghostty's true color support through to Neovim (termguicolors).
set-option -as terminal-features ',xterm-ghostty:RGB'
# Forward focus events so Neovim's autoread can reload externally changed files.
set-option -g focus-events on

# Mouse: click to select panes, drag borders to resize, wheel to scroll.
set-option -g mouse on
# Keep more scrollback history per pane.
set-option -g history-limit 50000

# Reload configuration without restarting the session.
bind-key r source-file /Users/hikari/.tmux.conf \; display-message 'tmux configuration reloaded'
